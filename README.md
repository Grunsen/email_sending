Small script that sends email using CSV table.


### Usage
Place `table.csv` in same folder as `main.py`.

Run
```
python main.py 
```
to send mail to all 

Run
```
python main.py -t
```
to send test email